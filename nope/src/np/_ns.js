np.define('np', (require) =>({
  Disposable: require('np.Disposable'),
  Event: require('np.Event'),
  List: require('np.List'),
  Observable: require('np.Observable'),
  Serializer: require('np.Serializer')
}));
