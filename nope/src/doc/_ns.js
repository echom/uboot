np.define('np.doc', (require) => ({
  Element: require('np.doc.Element'),
  List: require('np.doc.List'),
  Node: require('np.doc.Node'),
  Root: require('np.doc.Root'),
  Value: require('np.doc.Value')
}));
